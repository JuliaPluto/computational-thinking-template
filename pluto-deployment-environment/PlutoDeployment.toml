[Export]
baked_state = false
baked_notebookfile = false
offer_binder = true
slider_server_url = "http://localhost:1234/"
ignore_cache = [
    "index.jl",
]
create_index = false
exclude = [
    # these are in the repo but not used on the website
    "tools/*",
    "PlutoPages.jl",
]

[SliderServer]
port = 8080
host = "0.0.0.0"
exclude=[
    # these are in the repo but not used on the website
    "tools/*",
    "PlutoPages.jl",
    
    # notebooks not interactive

    # don't run homeworks
    "*/hw*.jl",
]
